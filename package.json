{
  "name": "@f/extract-opengraph",
  "description": "Extract opengraph metadata from a DOM (or dom-like thing, e.g. jsdom) into an object",
  "repository": "git://github.com/micro-js/extract-opengraph.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/reduce-array": "^0.2.1"
  },
  "devDependencies": {
    "jsdom": "^8.0.4",
    "tape": "^4.2.2"
  }
}
